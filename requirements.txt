pandas>=2.1
numpy>=1.26
networkx>=3.2
scipy>=1.11
matplotlib>=3.8
# Optional GIS extras — NOT imported by the codebase. Kept out of the default install so
# CI and fresh setups don't need GDAL/system libraries. Uncomment only if you add GIS features.
# geopandas>=0.14
# folium>=0.15
# pyproj>=3.6
# Streamlit app
streamlit>=1.38
pydeck>=0.9
pyyaml>=6.0
anthropic>=0.69
pydantic>=2
openai>=1.0
